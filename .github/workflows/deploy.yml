name: Deploy to Production

on:
  push:
    branches: [main, production]
  workflow_dispatch:

jobs:
  deploy:
    name: 🚀 Deploy to Production
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6

      - name: ℹ️ Info
        run: |
          echo "Promoting code to production branch."
          echo "Render will automatically pick up changes from the production branch."
